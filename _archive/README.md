# Archived files

These files are from earlier development phases and are not part of the current v2 architecture.
They are kept for reference only.

| Directory | Description |
|-----------|-------------|
| `python-v1/` | Python AI agents (Fase 5 plan — not integrated, Rust-only architecture) |
| `proto-unused/` | Protobuf definitions (replaced by serde JSON/MessagePack) |
| `CONSIGLI/` | Italian-language development advice notes |
| `seed_test_data.py` | Python test data seeder (replaced by Rust auto-seed) |
| `node_default.toml` | TOML config file (replaced by CLI args) |
